/** Max pages when importing a PDF — pages render sequentially so memory pressure is low */
export const MAX_PDF_PAGES = 300;

/** Default scale factor for PDF page rendering */
export const PDF_RENDER_SCALE = 2;

/** Max page dimension in PDF points (1pt = 1/72 inch). A4 width = 595pt. */
export const MAX_PAGE_POINTS = 595;
